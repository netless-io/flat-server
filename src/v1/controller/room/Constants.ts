export enum RoomType {
    OneToOne,
    SmallClass,
    BigClass,
}

export enum DocsType {
    Dynamic = "Dynamic",
    Static = "Static",
}

export enum Week {
    Sunday,
    Monday,
    Tuesday,
    Wednesday,
    Thursday,
    Friday,
    Saturday,
}

export enum RoomStatus {
    Pending = "Pending",
    Running = "Running",
    Stopped = "Stopped",
}

export enum ListType {
    All = "all",
    Today = "today",
    Periodic = "periodic",
    History = "history",
}
