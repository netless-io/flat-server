import { IORoutes, RestifyRoutes } from "./types/Server";

export const httpRoutes: Readonly<Readonly<RestifyRoutes[]>[]> = Object.freeze([]);

export const socketRoutes: Readonly<Readonly<IORoutes[]>[]> = Object.freeze([]);
